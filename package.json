{
  "name": "jizi-soundboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test",
    "check": "for f in script.js service-worker.js modules/*.js; do node --check \"$f\" || exit 1; done",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "tone": "^15.1.22"
  },
  "devDependencies": {
    "vite": "^7.0.0",
    "wrangler": "^4.0.0"
  }
}
